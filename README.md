# memory-driven-invoice-agent
This project implements a memory-based learning layer for invoice automation. The system learns from repeated human corrections and applies trusted memory to future invoices in an explainable, auditable way.

## Core Flow
Recall → Apply → Decide → Learn
